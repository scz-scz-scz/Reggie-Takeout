<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="scz.reggiecode1.mapper.CategoryMapper">
    <select id="getMaxId" resultType="java.lang.Long">
        select max(id) from category;
    </select>

    <insert id="save">
        insert into category (id, type, name, sort, create_time, update_time, create_user, update_user) values (#{id},#{type},#{name},#{sort},#{createTime},#{updateTime},#{createUser},#{updateUser});
    </insert>

    <select id="selectAll" resultType="java.lang.Integer">
        select count(*) from category;
    </select>

    <select id="list" resultType="scz.reggiecode1.entity.Category">
        select * from category order by sort,update_time desc limit #{index},#{pageSize};
    </select>

    <delete id="delete">
        delete from category where id=#{id};
    </delete>

    <update id="updateCategory">
        update category
        <set>
            <if test="name!=null">
                name=#{name},
            </if>
            <if test="sort!=null">
                sort=#{sort},
            </if>
            <if test="updateTime!=null and updateUser!=null">
                update_time=#{updateTime},update_user=#{updateUser}
            </if>
        </set>
        where id=#{id};
    </update>

    <select id="listByType" resultType="scz.reggiecode1.entity.Category">
        select * from category
        <where>
            <if test="type!=null">
                type=#{type}
            </if>
        </where>
        order by sort,update_time desc;
    </select>

    <select id="selectById" resultType="java.lang.String">
        select name from category where id=#{id};
    </select>
</mapper>