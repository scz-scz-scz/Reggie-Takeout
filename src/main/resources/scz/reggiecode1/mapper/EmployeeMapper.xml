<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="scz.reggiecode1.mapper.EmployeeMapper">
    <select id="login" resultType="scz.reggiecode1.entity.Employee">
        select id,name,username,password,status from employee where username=#{username} and password=#{password};
    </select>

    <select id="getMaxId" resultType="java.lang.Long">
        select max(id) from employee;
    </select>

    <insert id="save">
        insert into employee (id, name, username, password, phone, sex, id_number, create_time, update_time, create_user, update_user) values (#{id},#{name},#{username},#{password},#{phone},#{sex},#{idNumber},#{createTime},#{updateTime},#{createUser},#{updateUser});
    </insert>

    <select id="selectAll" resultType="java.lang.Integer">
        select count(*) from employee
        <if test="name!=null">
            where name like concat('%',#{name},'%')
        </if>
        ;
    </select>

    <select id="list" resultType="scz.reggiecode1.entity.Employee">
        select * from employee
        <where>
            <if test="name!=null">
                name like concat('%',#{name},'%')
            </if>
        </where>
        order by id limit #{index},#{pageSize};
    </select>

    <update id="updateEmployee">
        update employee
        <set>
            <if test="name!=null and username!=null and phone!=null and sex!=null and idNumber!=null">
                name=#{name},username=#{username},phone=#{phone},sex=#{sex},id_number=#{idNumber},
            </if>
            <if test="status!=null">
                status=#{status},
            </if>
            <if test="updateTime!=null and updateUser!=null">
                update_time=#{updateTime},update_user=#{updateUser},
            </if>
        </set>
        where id=#{id};
    </update>

    <select id="selectById" resultType="scz.reggiecode1.entity.Employee">
        select * from employee where id=#{id};
    </select>
</mapper>